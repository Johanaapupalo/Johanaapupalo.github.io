1. Explain the difference between flex-direction: row and flex-direction: column.
flex-direction row places items from left to right in a container, while flex-direction column places them from top to bottom.
2. Why is it important to use relative units (like %, vh, or rem) instead of fixed pixels (px) for
responsive design?
This units allow the website to rescale depending on what device we are using to acces it, the screen size of the device will change the scaling of it. So we use this relative units to adjsut to this changes in screen size.
3> AI contribution:
I keep getting 3 rows for my link even after following a step by step tutorial in youtube. So I send my code to chat to see what was wrong after changeing and reading my code for a long time. This is the prompt that I send it "why when i add the media the navlinks became 3 rows insead of just one row?". And apparently notjhing was wrong with my code, it was just that I have zoom in the page and I had the browser in split view, so it adquired the form it would have if it was tested on a phone.
